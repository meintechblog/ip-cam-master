---
quick_id: 260410-c36
description: Container-IP prominent im Batch-Onboarding + MAC im LXC-Panel anzeigen
date: 2026-04-10
status: code-complete (deploy deferred)
---

# Quick Task 260410-c36 — Summary

## What changed

### Task 1: Prominent container IP during batch onboarding
**File:** `src/routes/kameras/onboarding/+page.svelte`

- Added `containerIp?: string` field to `BatchCamera` interface
- Added `Copy` + `Check` to `lucide-svelte` imports
- In `batchOnboardPipeline()`: `batchResults[idx].containerIp = createData.containerIp;` assigned right after `/api/onboarding/create-container` success, before `setStep()`
- New highlighted header row renders inside the batch card whenever `cam.containerIp` is set — persists through all later steps and after `done`
- Row shows: label "Container-IP:", mono IP, Copy button (icon swaps to `Check` for 2s on click via `navigator.clipboard.writeText()`), and hint "→ Jetzt in UniFi Protect hinzufügbar" in `text-accent`

**Commit:** `b25b6e1` feat(quick-260410-c36-01): surface container IP prominently during batch onboarding

### Task 2: Populate LXC MAC via cached Proxmox config fetch
**Files:** `src/lib/types.ts`, `src/lib/server/services/proxmox.ts`, `src/routes/api/cameras/status/+server.ts`

- `ContainerInfo.mac?: string | null` added to types
- Module-level `macCache = new Map<number, string>()` in `proxmox.ts`
- Helper `parseMacFromNet0()` extracts `hwaddr=XX:XX:XX:XX:XX:XX` from net0 config string
- `listContainers()`: for vmids not in cache, fetch container config in parallel via `proxmox.nodes.$(node).lxc.$(vmid).config.$get()`, parse MAC, cache. Stale cleanup: entries whose vmid is no longer in `apiContainers` get removed via live-vmid Set diff. Try/catch isolates per-container failures.
- `getContainerStatus()`: reads from cache, falls back to a single config fetch if missing
- `/api/cameras/status`: replaced hardcoded `const lxcMac = null;` with `containerData?.mac ?? null`
- UI (`CameraDetailCard.svelte:283-288`) already handles the field — no UI change needed

**Commit:** `569062f` feat(quick-260410-c36-02): populate LXC MAC via cached Proxmox config fetch

### Task 3: VM deploy — DEFERRED
**Status:** VM `192.168.3.233` responds to ping but SSH (22) and app (3000) both refused.

Code is compile-clean and committed. Re-run manually when VM is back up:

```bash
rsync -avz --delete \
  --exclude='data/' \
  --exclude='.env' \
  --exclude='.git' \
  --exclude='node_modules' \
  --exclude='.svelte-kit' \
  ./ root@192.168.3.233:/opt/ip-cam-master/
ssh root@192.168.3.233 'cd /opt/ip-cam-master && npm install && npm run build && systemctl restart ip-cam-master'
```

## Notes

- **Svelte 5 reactivity:** `batchResults[idx].containerIp = …` works because `$state<BatchCamera[]>([])` proxies both the array and its element objects
- **MAC cache boundedness:** naturally bounded by the number of live LXC containers; cleanup via vmid diff inside `listContainers()` — no TTL/LRU
- **Additive only:** the existing step-detail line `LXC {vmid} @ {ip}` inside expanded details is untouched; the new prominent row is a separate header-area element
- **Worktree branch bug:** worktree was created from `790a8f4` instead of `22a0fba`; corrected via soft reset during execution
- **`npm run check`:** 9 pre-existing errors before and after — zero new errors introduced

## Files modified

- `src/routes/kameras/onboarding/+page.svelte`
- `src/lib/types.ts`
- `src/lib/server/services/proxmox.ts`
- `src/routes/api/cameras/status/+server.ts`

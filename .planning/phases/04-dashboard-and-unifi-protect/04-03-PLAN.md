---
phase: 04-dashboard-and-unifi-protect
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/routes/logs/+page.svelte
  - src/lib/components/events/EventTable.svelte
  - src/lib/components/events/EventFilters.svelte
  - src/lib/components/cameras/AdoptionGuide.svelte
autonomous: true
requirements: [DASH-04, ONBD-05]

must_haves:
  truths:
    - "Logs page shows filterable event table with camera, severity, type, and date filters"
    - "Logs page has Protect Logs tab showing raw UDM SSH log entries on demand"
    - "User can follow guided adoption instructions after clicking adopt button"
    - "Events display newest first with pagination"
  artifacts:
    - path: "src/routes/logs/+page.svelte"
      provides: "Active logs page with events tab and Protect logs tab"
      min_lines: 80
    - path: "src/lib/components/events/EventTable.svelte"
      provides: "Paginated event table component"
    - path: "src/lib/components/events/EventFilters.svelte"
      provides: "Filter controls for events"
    - path: "src/lib/components/cameras/AdoptionGuide.svelte"
      provides: "Step-by-step Protect adoption modal/panel"
  key_links:
    - from: "src/routes/logs/+page.svelte"
      to: "/api/protect/events"
      via: "fetch with filter params"
      pattern: "fetch.*api/protect/events"
    - from: "src/routes/logs/+page.svelte"
      to: "/api/logs/protect"
      via: "fetch for raw logs"
      pattern: "fetch.*api/logs/protect"
    - from: "src/lib/components/cameras/AdoptionGuide.svelte"
      to: "/api/protect/adopt"
      via: "POST to trigger adoption guide"
      pattern: "fetch.*api/protect/adopt"
---

<objective>
Activate the Logs page with a full filterable event table and raw Protect logs viewer. Create the guided adoption flow component for use in camera detail cards.

Purpose: Logs page replaces manual SSH debugging with self-service event browsing. Adoption guide delivers the "zu krass" one-click-ish Protect adoption experience. DASH-04 is already satisfied by existing snapshot/WebRTC preview, so this plan focuses on the remaining pieces.
Output: Active logs page with two tabs, event table/filter components, adoption guide component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-and-unifi-protect/04-CONTEXT.md
@.planning/phases/04-dashboard-and-unifi-protect/04-RESEARCH.md
@.planning/phases/04-dashboard-and-unifi-protect/04-01-SUMMARY.md

@src/routes/logs/+page.svelte
@src/lib/types.ts
@src/lib/components/cameras/CameraDetailCard.svelte

<interfaces>
<!-- API routes from Plan 02 -->

GET /api/protect/events?cameraId=N&severity=X&eventType=X&since=ISO&until=ISO&limit=N&offset=N
Response: { events: CameraEvent[], total: number }

GET /api/logs/protect?lines=N
Response: { logs: string, lines: number }

POST /api/protect/adopt body: { cameraId: number }
Response: { onvifRunning: boolean, containerReachable: boolean, instructions: string[], protectUrl: string }

<!-- Types from Plan 01 -->
```typescript
export interface CameraEvent {
  id: number; cameraId: number | null; cameraName: string | null;
  eventType: EventType; severity: EventSeverity; message: string;
  source: EventSource; timestamp: string;
}
export type EventType = 'camera_disconnect' | 'camera_reconnect' | 'stream_failed' | 'adoption_changed' | 'aiport_error';
export type EventSeverity = 'info' | 'warning' | 'error';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Logs page with event table, filters, and raw Protect logs tab</name>
  <files>src/routes/logs/+page.svelte, src/lib/components/events/EventTable.svelte, src/lib/components/events/EventFilters.svelte</files>
  <read_first>
    - src/routes/logs/+page.svelte (current placeholder: just h1 and "wird spaeter implementiert" text)
    - src/routes/+page.svelte (dashboard page — reference for Tailwind styling patterns, dark theme classes)
    - src/lib/types.ts (CameraEvent, EventType, EventSeverity types)
    - src/lib/components/layout/Sidebar.svelte (navigation — Logs link already present)
  </read_first>
  <action>
**Create `src/lib/components/events/EventFilters.svelte` (per D-25):**

Props: `{ cameras: { id: number, name: string }[], onFilter: (filters: EventFilterState) => void }`

Filter state type (define inline or in component):
```typescript
interface EventFilterState {
  cameraId: number | null;
  severity: EventSeverity | null;
  eventType: EventType | null;
  since: string; // date input value
  until: string; // date input value
}
```

UI: Horizontal flex row of filter controls, all using Tailwind dark theme classes (`bg-bg-input`, `border-border`, `text-text-primary`):
- Camera dropdown: `<select>` with "Alle Kameras" default + camera names from props
- Severity dropdown: `<select>` with "Alle" default, options: info, warning, error
- Event type dropdown: `<select>` with "Alle" default, options: camera_disconnect (Getrennt), camera_reconnect (Verbunden), stream_failed (Stream-Fehler), adoption_changed (Adoption), aiport_error (AI-Port Fehler)
- Date range: two `<input type="date">` for since/until
- Call `onFilter(currentState)` on any change.

**Create `src/lib/components/events/EventTable.svelte`:**

Props: `{ events: CameraEvent[], total: number, limit: number, offset: number, onPageChange: (newOffset: number) => void }`

Table with columns: Zeitpunkt, Schwere, Kamera, Typ, Nachricht.
- Zeitpunkt: Format as `new Date(event.timestamp).toLocaleString('de-DE', { day: '2-digit', month: '2-digit', hour: '2-digit', minute: '2-digit', second: '2-digit' })`
- Schwere: Colored dot — green for info, yellow for warning, red for error
- Kamera: `event.cameraName || 'System'`
- Typ: Map eventType to German label (camera_disconnect→Getrennt, camera_reconnect→Verbunden, stream_failed→Stream-Fehler, adoption_changed→Adoption, aiport_error→AI-Port)
- Nachricht: `event.message` (truncated to ~80 chars with title tooltip)

Pagination below table: "Seite X von Y" with Zurueck/Weiter buttons. Calculate pages from `total` and `limit`.

Styling: Same table pattern as dashboard camera table — `bg-bg-card rounded-xl border border-border`, header with uppercase tracking, hover rows.

**Replace `src/routes/logs/+page.svelte` (per D-24, D-25, D-26, D-27):**

Two tabs: "Ereignisse" (events) and "Protect Logs" (raw SSH logs).

Tab implementation: Simple `activeTab` state variable with two buttons styled as tabs (same pattern as existing settings page tabs — underline active).

**Ereignisse tab:**
- State: `events`, `totalEvents`, `cameras` (for filter dropdown), `filters`, `limit=50`, `offset=0`, `loading`
- On mount and filter change: `fetch('/api/protect/events?' + params)` where params built from filter state
- Also fetch `/api/cameras/status` once to populate camera names for filter dropdown
- Render `EventFilters` then `EventTable`

**Protect Logs tab (per D-27):**
- State: `rawLogs = ''`, `logLines = 100`, `logsLoading = false`
- Button "Logs laden" that fetches `GET /api/logs/protect?lines={logLines}`
- Display raw logs in a `<pre>` block with `font-mono text-xs text-text-secondary bg-bg-input rounded-lg p-4 overflow-x-auto max-h-[600px] overflow-y-auto whitespace-pre-wrap`
- Dropdown to select lines: 50, 100, 200, 500
- Note: "Direkte SSH-Verbindung zum UDM — zeigt /srv/unifi-protect/logs/cameras.thirdParty.log"
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/ip-cam-master && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/logs/+page.svelte exists and is NOT the placeholder (does not contain "wird spaeter implementiert")
    - src/routes/logs/+page.svelte contains `Ereignisse` tab text
    - src/routes/logs/+page.svelte contains `Protect Logs` tab text
    - src/routes/logs/+page.svelte contains `fetch('/api/protect/events`
    - src/routes/logs/+page.svelte contains `fetch('/api/logs/protect`
    - src/lib/components/events/EventTable.svelte exists
    - EventTable.svelte contains `Zeitpunkt` column header
    - EventTable.svelte contains pagination (Zurueck/Weiter or similar)
    - src/lib/components/events/EventFilters.svelte exists
    - EventFilters.svelte contains `Alle Kameras` default option
    - EventFilters.svelte contains severity select with info/warning/error
    - npm run build succeeds
  </acceptance_criteria>
  <done>Logs page is active with two tabs. Ereignisse tab shows paginated, filterable event table (by camera, severity, type, date range). Protect Logs tab shows raw UDM SSH log entries on demand with configurable line count.</done>
</task>

<task type="auto">
  <name>Task 2: Create guided adoption flow component and wire into CameraDetailCard</name>
  <files>src/lib/components/cameras/AdoptionGuide.svelte, src/lib/components/cameras/CameraDetailCard.svelte</files>
  <read_first>
    - src/lib/components/cameras/CameraDetailCard.svelte (full file — find the UniFi Protect section and the existing UI structure)
    - src/lib/types.ts (CameraCardData with protectStatus)
    - .planning/phases/04-dashboard-and-unifi-protect/04-RESEARCH.md (adoption flow: verify ONVIF then guided instructions)
  </read_first>
  <action>
**Create `src/lib/components/cameras/AdoptionGuide.svelte` (per D-08, D-09, D-10, ONBD-05):**

Props: `{ cameraId: number, cameraName: string, containerIp: string | null, onClose: () => void }`

State: `loading`, `result: { onvifRunning, containerReachable, instructions, protectUrl } | null`, `error: string | null`

On mount (via `$effect`): POST to `/api/protect/adopt` with `{ cameraId }`. Set result or error.

UI — Modal/panel overlay with dark backdrop:

1. **Loading state:** Spinner + "ONVIF-Server wird geprueft..."

2. **Success state (onvifRunning=true):**
   - Green check icon + "ONVIF-Server laeuft"
   - Numbered instruction list from `result.instructions` — each step in its own card-like row with step number badge (1, 2, 3, 4)
   - Direct link to Protect UI: `<a href={result.protectUrl} target="_blank">UniFi Protect oeffnen</a>` styled as primary button
   - "Schliessen" button

3. **Error state (onvifRunning=false):**
   - Red warning icon + "ONVIF-Server nicht erreichbar"
   - Message: "Der ONVIF-Server auf {containerIp} antwortet nicht. Starte den Container neu und versuche es erneut."
   - "Erneut pruefen" button that re-triggers the POST
   - "Schliessen" button

Styling: Same modal pattern as delete confirmation in CameraDetailCard — fixed overlay with `bg-black/50` backdrop, centered card with `bg-bg-card rounded-xl border border-border p-6`.

**Update `src/lib/components/cameras/CameraDetailCard.svelte`:**

1. Import AdoptionGuide component.
2. Add state: `let showAdoptionGuide = $state(false);`
3. In the UniFi Protect section of the card, when camera is NOT adopted (no `protectStatus` or `protectStatus.isAdopted === false`), add an "In Protect aufnehmen" button:
   ```html
   <button onclick={() => showAdoptionGuide = true}
     class="text-xs px-3 py-1.5 rounded-lg bg-accent/10 text-accent hover:bg-accent/20 transition-colors font-medium">
     In Protect aufnehmen
   </button>
   ```
4. Render the modal conditionally:
   ```html
   {#if showAdoptionGuide}
     <AdoptionGuide
       cameraId={camera.id}
       cameraName={camera.name}
       containerIp={camera.containerIp}
       onClose={() => showAdoptionGuide = false}
     />
   {/if}
   ```
5. For native ONVIF cameras (vmid === 0), also show the button but with different text: "Protect-Status pruefen" — since native cameras are adopted directly, the guide should show their current status or confirm they're already adopted.
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/ip-cam-master && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/components/cameras/AdoptionGuide.svelte exists
    - AdoptionGuide.svelte contains `fetch('/api/protect/adopt`
    - AdoptionGuide.svelte contains `ONVIF-Server wird geprueft` loading text
    - AdoptionGuide.svelte contains `ONVIF-Server laeuft` success text
    - AdoptionGuide.svelte contains `ONVIF-Server nicht erreichbar` error text
    - AdoptionGuide.svelte contains `Erneut pruefen` retry button
    - AdoptionGuide.svelte contains `UniFi Protect oeffnen` link
    - CameraDetailCard.svelte contains `showAdoptionGuide` state
    - CameraDetailCard.svelte contains `In Protect aufnehmen` button text
    - CameraDetailCard.svelte contains `AdoptionGuide` component reference
    - npm run build succeeds
  </acceptance_criteria>
  <done>AdoptionGuide component verifies ONVIF server, shows step-by-step German instructions, and links to Protect UI. CameraDetailCard has "In Protect aufnehmen" button for non-adopted cameras that opens the guide. Native ONVIF cameras also have Protect status check.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Logs page replaces placeholder with functional event table and raw logs viewer
- AdoptionGuide component renders instructions from API
- CameraDetailCard includes adoption button for non-adopted cameras
</verification>

<success_criteria>
- Logs page has two tabs: Ereignisse (filterable events) and Protect Logs (raw SSH logs)
- Event table supports filtering by camera, severity, type, and date range
- Events display newest first with pagination controls
- Protect Logs tab loads raw log lines on demand from UDM via SSH
- "In Protect aufnehmen" button in camera cards opens guided adoption flow
- Adoption guide verifies ONVIF server before showing instructions
- All German UI text matches CONTEXT.md decisions
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-unifi-protect/04-03-SUMMARY.md`
</output>

---
phase: 22
plan: 06
slug: live-vm-uat
type: execute
wave: 4
depends_on: [22-01, 22-02, 22-03, 22-04, 22-05]
files_modified: []
autonomous: false
requirements:
  - HUB-WIZ-05
  - HUB-WIZ-06
  - HUB-WIZ-07
  - HUB-WIZ-08
  - HUB-WIZ-09
  - HUB-WIZ-10
  - HUB-UI-01
  - HUB-UI-02
  - HUB-UI-03
  - HUB-UI-04
  - HUB-UI-05
  - HUB-UI-06
  - HUB-UI-07
  - HUB-UI-08
tags: [protect-hub, uat, live-vm, p22]
created: 2026-05-07

must_haves:
  truths:
    - "All 9 ROADMAP P22 success criteria are visually verified against the live VM at 192.168.3.178:3000"
    - "Wizard end-to-end run completes in ~5 minutes for a representative cam set"
    - "Resumability is verified: SvelteKit kill mid-Step-5 + restart + re-entry resumes at last completed step"
    - "/kameras partition renders correctly post-onboarding; no LXC 0 + red dot bug on external rows"
    - "External cam detail card shows Protect-Hub badge + qualifier + Outputs subsection + ProtectHubGuide; cam-edit/cam-delete are gone"
    - "All-URLs page lists every active output URL with working copy buttons"
    - "Settings → Protect Hub tab status panel + event log render live data; Sync-now triggers a reconcile"
  artifacts:
    - path: ".planning/phases/22-onboarding-wizard-cameras-integration/22-06-UAT-EVIDENCE.md"
      provides: "UAT evidence log with checklist + screenshots/CLI output per success criterion"
  key_links:
    - from: "Live VM (ip-cam-master alias)"
      to: "Bridge LXC vmid 2014 @ 192.168.3.139"
      via: "Reconcile produces YAML + go2rtc serves on :1984/:8554"
      pattern: "192.168.3.139"
---

<objective>
Verify all 9 ROADMAP P22 success criteria + the 14 requirements end-to-end against the live VM at `192.168.3.178:3000` (ssh alias `ip-cam-master`). The bridge LXC vmid 2014 @ 192.168.3.139 is already running from P21 with Loxone-MJPEG + Frigate-RTSP streams flowing. After git push triggers auto-deploy (per memory feedback_deploy_to_vm.md), this plan walks the user through every visible surface introduced by P22.

Purpose: P22 is overwhelmingly UI work — Playwright is not installed, jsdom is not installed, regex tests verify file structure but not pixels. The live UAT is the only way to confirm the user-facing flow works end-to-end. This mirrors the P20 Plan 03 + P21 Plan 06 pattern.

Output: `22-06-UAT-EVIDENCE.md` with one-line PASS/FAIL per checklist item, plus narrative notes for any failures or surprises discovered during the run.

Before starting: ensure all of Plans 01-05 have been merged to main, the dev-deploy push is complete, and the VM has restarted to pick up the changes (`systemctl status ip-cam-master` on the VM confirms running with the new SHA).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-onboarding-wizard-cameras-integration/22-CONTEXT.md
@.planning/phases/22-onboarding-wizard-cameras-integration/22-RESEARCH.md
@.planning/phases/22-onboarding-wizard-cameras-integration/22-UI-SPEC.md
@.planning/phases/22-onboarding-wizard-cameras-integration/22-01-PLAN.md
@.planning/phases/22-onboarding-wizard-cameras-integration/22-02-PLAN.md
@.planning/phases/22-onboarding-wizard-cameras-integration/22-03-PLAN.md
@.planning/phases/22-onboarding-wizard-cameras-integration/22-04-PLAN.md
@.planning/phases/22-onboarding-wizard-cameras-integration/22-05-PLAN.md

<interfaces>
<!-- Live VM access -->
- App URL: http://192.168.3.178:3000 (LAN)
- SSH: `ssh ip-cam-master` (alias works per memory feedback_github_ssh.md)
- Bridge LXC: vmid 2014 @ 192.168.3.139 (running from P21)
- Auto-deploy: `git push origin main` triggers VM redeploy via `./scripts/dev-deploy.sh` (per memory feedback_deploy_to_vm.md)
- DB: `data/db.sqlite` on VM; can be inspected via `ssh ip-cam-master 'sqlite3 /path/to/data/db.sqlite ...'`
- App service restart: `ssh ip-cam-master 'systemctl restart ip-cam-master'`

<!-- 9 ROADMAP P22 Success Criteria (must verify each) -->
1. Wizard 6 steps complete end-to-end (~3 min for 4 cams) — Step 3 fetches + populates, Step 4 first-party pre-checked + third-party with copy hint, Step 5 first-reconcile + go2rtc-healthy poll, Step 6 redirects to /kameras with toast
2. Wizard resumable: kill mid-Step-5 (SIGTERM), restart, re-open wizard → resume at last completed step
3. settings.protect_hub_enabled flips ONLY after Step 6: aborting at Step 4 leaves toggle OFF; /kameras shows zero external rows
4. Toggle flapping prevented: hub_state in {starting, stopping} → toggle grayed + spinner + non-cancellable; explicit "Abbrechen" only path to abort
5. /kameras single merged sorted array; external cards show "Protect Hub" + qualifier badge
6. External detail page: read-only catalog + active outputs with toggles + URL copy buttons + snapshot preview; cam-edit/cam-delete hidden; "Aus Hub entfernen" replaces "delete container" (DISABLED in P22 — verify the disabled state with tooltip)
7. Each output URL has copy button + per-target ProtectHubGuide snippet (Loxone + Frigate, pre-filled)
8. /settings/protect-hub/all-urls lists every active output URL grouped by output type + per-output copy
9. Settings → Protect Hub tab shows: Hub status, last reconcile, container badge, drift indicator (only when applicable), Sync-now button, last 50 reconcile events
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm VM is on the new SHA + reset DB state</name>
  <files></files>
  <action>
    Before opening the browser, confirm the VM is running the latest code AND the DB is in a clean pre-onboarding state for the wizard run.

    1. Confirm VM SHA matches origin/main:
    ```sh
    LOCAL_SHA=$(git rev-parse HEAD)
    VM_SHA=$(ssh ip-cam-master 'cd /opt/ip-cam-master && git rev-parse HEAD' 2>/dev/null)
    echo "Local: $LOCAL_SHA"
    echo "VM:    $VM_SHA"
    ```
    Replace `/opt/ip-cam-master` with the actual VM repo path if different (resolve via `systemctl cat ip-cam-master` to find WorkingDirectory).
    If SHAs differ: `git push origin main` and wait for `journalctl -u ip-cam-master -f` on the VM to show "successfully restarted".

    2. Confirm `hub_onboarding_state` table exists in VM DB:
    ```sh
    ssh ip-cam-master 'sqlite3 /opt/ip-cam-master/data/db.sqlite ".schema hub_onboarding_state"'
    ```
    Should return the CREATE TABLE statement. If empty, drizzle-kit push hasn't applied — Plan 01 Task 3 BLOCKING was incomplete.

    3. Reset wizard state for a clean run:
    ```sh
    ssh ip-cam-master 'sqlite3 /opt/ip-cam-master/data/db.sqlite "DELETE FROM hub_onboarding_state; UPDATE settings SET value=\"false\" WHERE key=\"protect_hub_enabled\";"'
    ```
    This puts the wizard back at Step 3 (bridge already exists from P21, so Step 1+2 auto-skip; pointer is null so resume banner won't render).

    4. Confirm bridge is healthy:
    ```sh
    ssh ip-cam-master 'curl -s http://192.168.3.139:1984/api/streams | head -c 200'
    ```
    Should return JSON listing existing streams (from P21 Loxone-MJPEG + Frigate-RTSP outputs already configured).
  </action>
  <verify>
    <automated>ssh ip-cam-master 'sqlite3 /opt/ip-cam-master/data/db.sqlite ".schema hub_onboarding_state" | grep -c hub_onboarding_state'</automated>
  </verify>
  <done>
    - Local + VM SHAs match
    - `hub_onboarding_state` table exists on VM
    - Wizard state cleared (no in-progress pointer)
    - Bridge LXC is healthy + go2rtc serving
    - Create `.planning/phases/22-onboarding-wizard-cameras-integration/22-06-UAT-EVIDENCE.md` with a "Pre-flight" section recording the SHAs + bridge health output
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — Wizard end-to-end (Steps 3-6, success criteria 1, 3, 4)</name>
  <what-built>
    Plans 01-05 ship: Wizard Steps 3-6 + WizardStepIndicator + WizardResumeBanner + atomic complete endpoint + /kameras toast banner.
  </what-built>
  <how-to-verify>
    Open http://192.168.3.178:3000 in a browser.

    1. Navigate to `/settings` → click "Protect Hub" tab → confirm the Hub-Status panel + event log are present (UI surface from Plan 05 verifies that the tab now embeds the new components).

    2. From the settings tab, click the "Protect Hub aktivieren" toggle. The app should navigate to `/settings/protect-hub/onboarding`. Verify:
       - Step indicator at top shows 6 discs; Steps 1+2 are GREEN (completed — bridge exists from P21); Step 3 is BLUE (current)
       - No resume banner is rendered (pointer was cleared in Pre-flight)

    3. **Step 3** auto-fetches catalog: spinner "Kameras werden gelesen…" → success summary "N Kameras gefunden — M erstanbieter, K drittanbieter." Click "Auswahl übernehmen".
       - VERIFY: pointer advances (`ssh ip-cam-master 'sqlite3 ... "SELECT * FROM hub_onboarding_state"'` returns step=3)

    4. **Step 4** renders Erstanbieter (UniFi) section + Drittanbieter section. Verify:
       - First-party cams have checkbox PRE-CHECKED + output dropdown defaulted to Loxone-MJPEG (640×360@10)
       - Third-party cams (e.g., Mobotix S15 if present) have checkbox UNCHECKED + hint copy "Liefert die Cam bereits MJPEG nativ? Dann hier aus lassen."
       - Toggle a third-party cam ON; if MJPEG count reaches 4, soft warning appears
       - Try to enable a 7th MJPEG (if you have ≥7 cams; otherwise simulate by enabling Frigate-RTSP on multiple — Frigate is uncounted)
       - Click "Auswahl übernehmen" → advances to Step 5

    5. **Step 5** runs first reconcile. Verify three named stages render in order:
       - "YAML wird geschrieben…" → "YAML auf Bridge" (green check)
       - "go2rtc wird neu geladen…" → "go2rtc bereit"
       - "Streams werden geprüft…" → "Streams laufen"
       - 1500 ms cadence (visible by spinner heartbeat)
       - On success → "Zum Abschluss" CTA appears

    6. **Test resumability (success criterion 2):** BEFORE clicking "Zum Abschluss", from a separate terminal:
       ```sh
       ssh ip-cam-master 'systemctl restart ip-cam-master'
       ```
       Wait ~10 s for service restart. Refresh the browser tab → wizard should re-load with the resume banner rendered: "Du warst bei Schritt N — weiter?" with relative time. Click "Weiter zu Schritt N" → land on Step 5 (which re-runs reconcile if needed) or Step 6 if reconcile already completed before kill.

    7. **Step 6** confirmation card → click "Zur Kameraliste". Verify:
       - URL transitions to /kameras?onboarding=success
       - Green toast banner appears: "Protect Hub aktiv — N Streams laufen."
       - Banner auto-dismisses after ~5 s; URL drops the query param (refresh does NOT re-trigger)

    8. **Verify success criterion 3 (atomic enable):**
       ```sh
       ssh ip-cam-master 'sqlite3 /opt/ip-cam-master/data/db.sqlite "SELECT key, value FROM settings WHERE key=\"protect_hub_enabled\""'
       ```
       Should now return `protect_hub_enabled|true`.

    9. **Verify success criterion 4 (toggle flap protection):**
       - Open `/settings → Protect Hub` tab
       - During the wizard run (steps 3-5 in-flight), the settings toggle (if visible from another tab) should be greyed-out with the L-18 "Vorgang läuft…" caption (this is provided by Plan 05's HubStatusPanel reading hub-state.ts derived state — verify by inspection)

    10. **Verify success criterion 3 (abort at Step 4):**
       Reset DB: `ssh ip-cam-master 'sqlite3 ... "DELETE FROM hub_onboarding_state; UPDATE settings SET value=\"false\" WHERE key=\"protect_hub_enabled\";"'`. Re-open wizard → advance to Step 4 → close browser tab. Inspect DB: `protect_hub_enabled` is still 'false'; `hub_onboarding_state` shows step=3 (Step 3 was the last to complete). Open `/kameras` → external section is NOT rendered (hubEnabled=false).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 sub-checks pass; otherwise list each failure with the step number + screenshot + observed behavior. Append findings to `22-06-UAT-EVIDENCE.md` under "Wizard end-to-end" section.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — /kameras partition + ExternalCamCard (success criteria 5, 6, 7)</name>
  <what-built>
    Plans 03 + 05 ship: /kameras two-section partition, ExternalCamCard with badges + catalog table + outputs + ProtectHubGuide, LXC 0 bug fix.
  </what-built>
  <how-to-verify>
    Pre-condition: wizard from Task 2 completed; protect_hub_enabled=true in DB.

    Open http://192.168.3.178:3000/kameras.

    1. **Partition (criterion 5):** Page shows two sections:
       - "Eigene Kameras (N)" — managed cams (Mobotix, Loxone, Bambu)
       - "Aus UniFi Protect (M)" — external Protect cams (only present because hubEnabled=true)
       - The pre-existing h1 "Kameras" is preserved verbatim (`text-2xl font-bold`)

    2. **Badges (criterion 5):** Each external cam card shows TWO badges in the top row:
       - Primary "Protect Hub" badge (accent border + `bg-accent/15 text-accent border border-accent/30 px-2 py-1 rounded text-xs`)
       - Qualifier badge: "UniFi" for first-party UVC cams; "Drittanbieter · {manufacturer}" for third-party (e.g., "Drittanbieter · BobtheBuilder" for the Bambu mirror cam id=23)

    3. **LXC 0 bug FIXED (criterion 6):** No external row shows the "LXC 0" + red status dot anymore. The LXC chrome is GONE; the right column shows the read-only Stream Catalog table (3-col Channel · Codec · Resolution@FPS).

    4. **External cam detail (criterion 6):**
       - Cam-edit and cam-delete buttons are HIDDEN
       - Action menu shows "Aus Hub entfernen" but it's DISABLED with tooltip "Verfügbar in Phase 23"

    5. **Outputs subsection (criterion 6):**
       - Two toggle rows: Loxone-MJPEG (sub-label "640×360 · 10 fps · transcodiert (VAAPI)") and Frigate-RTSP (sub-label "Passthrough · ohne Audio")
       - Toggle ON for Loxone-MJPEG → URL appears underneath: `Adresse: http://192.168.3.139:1984/api/stream.mjpeg?src=<mac>-low` with copy button
       - Click copy → check icon flashes green for 2 s
       - Paste in a terminal → verify exact URL match
       - Open the URL in VLC (or curl HEAD) → verify stream is reachable

    6. **Snapshot preview (criterion 6):** External card shows the snapshot image fetched from `:1984/api/frame.jpeg?src=<mac>-low&t=...`. Click the reload icon (top-right of preview area) → image refreshes (cache-buster updates).

    7. **ProtectHubGuide (criterion 7):**
       - Per cam, below Outputs, an "Anleitung — Stream einbinden" panel with 2 tabs: "Loxone (Intercom)" + "Frigate (NVR)"
       - Loxone tab shows pre-filled snippet with German `#`-comments + URL line; copy button works
       - Frigate tab shows pre-filled YAML cameras: block with commented detect/record hints; copy button works
       - Verify `{mac}-low` and `{mac}-high` placeholders are SUBSTITUTED (no template literal leakage in the rendered output)
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 sub-checks pass; otherwise log each failure in `22-06-UAT-EVIDENCE.md` under "/kameras + ExternalCamCard".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — All-URLs page + Hub-Tab status panel + event log (success criteria 8, 9)</name>
  <what-built>
    Plan 05 ships: `/settings/protect-hub/all-urls` + HubStatusPanel + HubEventLog.
  </what-built>
  <how-to-verify>
    1. **All-URLs page (criterion 8):** Open http://192.168.3.178:3000/settings/protect-hub/all-urls. Verify:
       - Breadcrumb back-link "← Zurück zu Einstellungen" present
       - h1 "Hub-Adressen — Übersicht" (`text-2xl font-semibold` — NOT bold)
       - Subtitle "Alle aktiven Stream-Adressen, gruppiert nach Ausgangstyp."
       - Group 1 "Loxone-MJPEG (N)" with one row per active loxone-mjpeg output: cam name + slug + mono URL + copy button
       - Group 2 "Frigate-RTSP (M)" same structure
       - Empty groups (if no outputs of that type enabled) show "Keine Ausgänge dieses Typs aktiv."
       - Click any copy button → check icon flashes green; paste in terminal → URL matches the per-cam-detail URL exactly

    2. **All-URLs empty-page state:** Temporarily disable hub: `ssh ip-cam-master 'sqlite3 ... "UPDATE settings SET value=\"false\" WHERE key=\"protect_hub_enabled\""'`. Refresh `/settings/protect-hub/all-urls` → page shows "Protect Hub ist nicht aktiv. → Im Einstellungs-Tab \"Protect Hub\" aktivieren." with link. Re-enable: `... value="true"...`.

    3. **Hub-Status panel (criterion 9):** Open http://192.168.3.178:3000/settings → click Protect Hub tab. Verify the new "Hub-Status" panel renders:
       - Bridge state row: green dot + "Bridge: running" + mono value
       - Last reconcile: relative time "vor X Minuten"
       - Konfig-Hash: 8-char prefix + `…`
       - Aktive Streams: integer (matches `curl http://192.168.3.139:1984/api/streams | jq 'keys | length'`)
       - Drift indicator: NOT rendered (P22 stub returns driftDetected=false; this is expected)
       - Sync-now button "Jetzt synchronisieren" present

    4. **Sync-now (criterion 9):** Click "Jetzt synchronisieren". Verify:
       - Button label changes to "Synchronisation läuft…" with spinner; button is disabled
       - Within ~5-10 s the button reverts to "Jetzt synchronisieren"
       - Last reconcile timestamp updates to "vor 0 Minuten" / "soeben"

    5. **Event log (criterion 9):** Below status panel, "Letzte Ereignisse" panel:
       - Renders divide-y grid: timestamp · event-type badge · success/failed · reconcile-id[0:8]
       - Should show at least the recent Sync-now reconcile + the wizard's first reconcile + any 5-min scheduler ticks
       - Error rows (if any) tinted `text-danger`
       - Polls every 10 s — leave the page open for 30+ s to see new rows arrive

    6. **Cross-tab smoke:** Open `/settings → Protect Hub` in two tabs. Click Sync-now in tab A; tab B should reflect the in-flight state within 10 s (next poll cycle) — its Sync-now button also gets disabled. After completion, both tabs show updated last-reconcile timestamp.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 sub-checks pass; otherwise log failures in `22-06-UAT-EVIDENCE.md` under "All-URLs + Hub-Tab".</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Finalize UAT evidence + close phase</name>
  <files>
    .planning/phases/22-onboarding-wizard-cameras-integration/22-06-UAT-EVIDENCE.md
  </files>
  <action>
    After Tasks 2-4 are all "approved", finalize the UAT evidence document. Structure:

    ```markdown
    # P22 UAT Evidence — Live VM (192.168.3.178:3000)

    **Date:** 2026-05-XX
    **Tester:** {user name or "claude-uat"}
    **VM SHA:** {sha}
    **Bridge:** vmid 2014 @ 192.168.3.139

    ## Pre-flight
    - [x] VM SHA matches origin/main: {sha}
    - [x] hub_onboarding_state table exists on VM
    - [x] Bridge healthy: {curl output}

    ## Success Criteria

    | # | ROADMAP Criterion | Result | Evidence |
    |---|-------------------|--------|----------|
    | 1 | 6-step wizard end-to-end ~3 min for 4 cams | PASS | screenshots: wizard-step3.png, ..., wizard-step6.png |
    | 2 | Resumable across SvelteKit restart | PASS | resume-banner.png after `systemctl restart` |
    | 3 | protect_hub_enabled flips ONLY after Step 6 | PASS | DB inspection before/after Step 6 |
    | 4 | Toggle flap-protection during transitions | PASS | settings-tab-toggle-disabled.png |
    | 5 | /kameras merged + Protect Hub badges | PASS | kameras-partition.png |
    | 6 | External detail page (catalog + outputs + buttons) | PASS | externalcamcard-detail.png |
    | 7 | Per-output copy + ProtectHubGuide snippets | PASS | hub-guide-loxone.png, hub-guide-frigate.png |
    | 8 | /settings/protect-hub/all-urls | PASS | all-urls.png |
    | 9 | Settings tab status panel + event log | PASS | hub-tab-status.png, event-log.png |

    ## Requirement Coverage

    | REQ-ID | Verified By | Status |
    |--------|-------------|--------|
    | HUB-WIZ-05 | Task 2 sub-check 3 | PASS |
    | HUB-WIZ-06 | Task 2 sub-check 4 | PASS |
    | HUB-WIZ-07 | Task 2 sub-check 5 | PASS |
    | HUB-WIZ-08 | Task 2 sub-check 7 | PASS |
    | HUB-WIZ-09 | Task 2 sub-check 6 | PASS |
    | HUB-WIZ-10 | Task 2 sub-checks 7,8,10 | PASS |
    | HUB-UI-01 | Task 3 sub-check 1 | PASS |
    | HUB-UI-02 | Task 3 sub-check 2 | PASS |
    | HUB-UI-03 | Task 3 sub-checks 5,6 | PASS |
    | HUB-UI-04 | Task 3 sub-check 4 | PASS |
    | HUB-UI-05 | Task 3 sub-check 4 | PASS |
    | HUB-UI-06 | Task 3 sub-check 7 | PASS |
    | HUB-UI-07 | Task 4 sub-checks 1,2 | PASS |
    | HUB-UI-08 | Task 4 sub-checks 3,4,5 | PASS |

    ## Surprises / Findings (if any)
    - {note any deviations from spec, source bugs found mid-UAT, P22 follow-up items}

    ## Status
    - [ ] All 9 success criteria PASS
    - [ ] All 14 requirements PASS
    - [ ] Evidence captured
    ```

    Replace bracketed placeholders with actual values. Attach screenshots to `.planning/phases/22-onboarding-wizard-cameras-integration/screenshots/` (create dir if absent). On any FAIL: open a `/gsd:debug` session per project convention OR open a P22 follow-up plan (`/gsd:plan-phase 22 --gaps`) — DO NOT close the phase with failures.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-onboarding-wizard-cameras-integration/22-06-UAT-EVIDENCE.md</automated>
  </verify>
  <done>
    - 22-06-UAT-EVIDENCE.md exists with all checkboxes ticked
    - All 9 ROADMAP success criteria marked PASS (or, if FAIL, follow-up plan opened)
    - All 14 P22 requirements marked PASS
    - Screenshots committed (or, if explicit user-decision, evidence is text-only via curl outputs + DB inspections)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → VM via SSH | Authenticated SSH (alias `ip-cam-master`); LAN-only access. |
| Browser → VM @ 192.168.3.178:3000 | LAN per L-23. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-22 | Information Disclosure | UAT screenshots may include bridge IPs / cam MACs in copy-snippets | accept | Screenshots stored in `.planning/phases/22-.../screenshots/` (committed to private repo if applicable; LAN data per L-23). If repo is public, redact bridge IP + MAC before commit. |

UAT does not introduce code; threat model is preserved from Plans 01-05.
</threat_model>

<verification>
- All 9 ROADMAP success criteria PASS (Tasks 2, 3, 4)
- All 14 P22 requirements verified (per Tasks 2-4 sub-checks)
- `22-06-UAT-EVIDENCE.md` committed
- No FAIL items remain (or, if any, a follow-up plan is opened)
- VM is in a clean post-onboarding state (protect_hub_enabled=true; hub_onboarding_state status=completed)
</verification>

<success_criteria>
1. All 9 ROADMAP P22 success criteria visually verified against the live VM
2. UAT evidence document created with PASS/FAIL per checklist + screenshots
3. No regressions on P19/P20/P21 functionality (existing managed cams still work; bridge LXC still serves; reconciler 5-min tick still healthy)
4. Phase 22 ready to mark complete in STATE.md + ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/22-onboarding-wizard-cameras-integration/22-06-SUMMARY.md` summarizing the UAT outcome and updating the ROADMAP P22 entry to COMPLETE.
</output>

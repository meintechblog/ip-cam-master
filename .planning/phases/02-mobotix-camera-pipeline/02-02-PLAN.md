---
phase: 02-mobotix-camera-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/routes/kameras/+page.svelte
  - src/routes/kameras/onboarding/+page.svelte
  - src/routes/kameras/onboarding/+page.server.ts
  - src/lib/components/onboarding/OnboardingWizard.svelte
  - src/lib/components/onboarding/StepIndicator.svelte
  - src/lib/components/onboarding/StepCredentials.svelte
  - src/lib/components/onboarding/StepTestConnection.svelte
  - src/lib/components/onboarding/StepCreateContainer.svelte
  - src/lib/components/onboarding/StepConfigureGo2rtc.svelte
  - src/lib/components/onboarding/StepVerifyStream.svelte
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-06

must_haves:
  truths:
    - "User can click '+Kamera hinzufuegen' on the Kameras page and be taken to the onboarding wizard"
    - "Wizard shows 5 steps with a progress indicator at the top"
    - "Step 1 lets user enter camera IP, credentials, and optional transcode parameters with smart defaults"
    - "Each step shows loading state during API call and error with retry button on failure"
    - "Step 5 shows WebRTC live preview via go2rtc iframe and displays RTSP URL"
    - "After completion, user returns to Kameras page where new camera appears"
  artifacts:
    - path: "src/routes/kameras/+page.svelte"
      provides: "+Kamera hinzufuegen button linking to /kameras/onboarding"
      contains: "Kamera hinzuf"
    - path: "src/routes/kameras/onboarding/+page.svelte"
      provides: "Onboarding wizard page"
      contains: "OnboardingWizard"
    - path: "src/lib/components/onboarding/OnboardingWizard.svelte"
      provides: "Main wizard container managing currentStep state and step transitions"
      contains: "currentStep"
    - path: "src/lib/components/onboarding/StepIndicator.svelte"
      provides: "5-step progress bar"
      contains: "step"
    - path: "src/lib/components/onboarding/StepCredentials.svelte"
      provides: "Form for camera IP, username, password, transcode params"
      contains: "ip"
    - path: "src/lib/components/onboarding/StepVerifyStream.svelte"
      provides: "WebRTC iframe preview + RTSP URL display"
      contains: "iframe"
  key_links:
    - from: "src/routes/kameras/+page.svelte"
      to: "/kameras/onboarding"
      via: "anchor or goto"
      pattern: "kameras/onboarding"
    - from: "src/lib/components/onboarding/OnboardingWizard.svelte"
      to: "/api/onboarding/*"
      via: "fetch calls in step handlers"
      pattern: "fetch.*api/onboarding"
    - from: "src/lib/components/onboarding/StepVerifyStream.svelte"
      to: "go2rtc stream.html"
      via: "iframe src"
      pattern: "stream\\.html\\?src="
---

<objective>
Build the 5-step onboarding wizard UI for Mobotix camera onboarding.

Purpose: This is the user-facing flow that calls the backend services from Plan 01. The wizard walks through: enter credentials -> test connection -> create container -> configure go2rtc -> verify stream. Per D-02, it's a 5-step stepper with progress indicator (D-03), retry on error (D-04), and return to Kameras page on completion (D-05).

Output: Onboarding wizard page at /kameras/onboarding with 7 Svelte components (wizard + indicator + 5 step components), updated Kameras page with "+Kamera hinzufuegen" button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mobotix-camera-pipeline/02-CONTEXT.md
@.planning/phases/02-mobotix-camera-pipeline/02-RESEARCH.md
@.planning/phases/02-mobotix-camera-pipeline/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -- API routes the wizard calls -->

POST /api/onboarding/test-connection
  Request: { ip: string, username: string, password: string }
  Response: { success: boolean, resolution?: string, fps?: number, streamPath?: string, error?: string }

POST /api/onboarding/create-container
  Request: { cameraId: number }
  Response: { success: boolean, vmid?: number, containerIp?: string, error?: string }

POST /api/onboarding/configure-go2rtc
  Request: { cameraId: number }
  Response: { success: boolean, error?: string }

POST /api/onboarding/verify-stream
  Request: { cameraId: number }
  Response: { success: boolean, rtspUrl?: string, streamInfo?: { active: boolean, codec: string|null, producers: number }, error?: string }

<!-- From Plan 01 outputs -- types -->
From src/lib/types.ts:
```typescript
export type CameraStatus = 'pending' | 'container_created' | 'configured' | 'verified';
export interface TranscodeParams { width: number; height: number; fps: number; bitrate: number; }
export interface Camera { id, vmid, name, ip, username, password, cameraType, streamPath, width, height, fps, bitrate, streamName, rtspUrl, containerIp, status, createdAt, updatedAt }
export interface StreamInfo { active: boolean; codec: string | null; producers: number; resolution: string | null; }
```

<!-- From Plan 01 outputs -- onboarding service functions called by API routes -->
From src/lib/server/services/onboarding.ts:
```typescript
export async function saveCameraRecord(params): Promise<number>;  // returns camera id
export async function getNextVmid(): Promise<number>;
```

<!-- Existing UI patterns -->
From src/lib/components/ui/InlineAlert.svelte:
```svelte
<InlineAlert type="error" message={errorMessage} />
<InlineAlert type="success" message={successMessage} />
```

From src/routes/kameras/+page.svelte:
```svelte
<h1 class="text-2xl font-bold text-text-primary mb-4">Kameras</h1>
<ContainerGrid containers={data.containers} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding wizard components + Kameras page button</name>
  <files>
    src/routes/kameras/+page.svelte,
    src/routes/kameras/onboarding/+page.svelte,
    src/routes/kameras/onboarding/+page.server.ts,
    src/lib/components/onboarding/OnboardingWizard.svelte,
    src/lib/components/onboarding/StepIndicator.svelte,
    src/lib/components/onboarding/StepCredentials.svelte,
    src/lib/components/onboarding/StepTestConnection.svelte,
    src/lib/components/onboarding/StepCreateContainer.svelte,
    src/lib/components/onboarding/StepConfigureGo2rtc.svelte,
    src/lib/components/onboarding/StepVerifyStream.svelte
  </files>
  <read_first>
    src/routes/kameras/+page.svelte,
    src/routes/kameras/+page.server.ts,
    src/lib/components/containers/ContainerGrid.svelte,
    src/lib/components/containers/ContainerCard.svelte,
    src/lib/components/ui/InlineAlert.svelte,
    src/lib/types.ts,
    src/app.css,
    src/routes/+layout.svelte,
    src/routes/einstellungen/+page.svelte
  </read_first>
  <action>
    **Per D-01:** Add "+Kamera hinzufuegen" button to Kameras page.
    **Per D-02, D-03, D-04, D-05:** Build 5-step wizard with progress indicator, retry on error, return to Kameras on completion.

    **1. Update Kameras page** (`src/routes/kameras/+page.svelte`):
    Add a header row with flex justify-between. The h1 "Kameras" on the left, a link/button on the right:
    ```svelte
    <div class="flex items-center justify-between mb-4">
      <h1 class="text-2xl font-bold text-text-primary">Kameras</h1>
      <a href="/kameras/onboarding" class="inline-flex items-center gap-2 px-4 py-2 bg-accent text-white rounded-lg hover:bg-accent/90 transition-colors">
        + Kamera hinzufuegen
      </a>
    </div>
    ```
    Keep the existing error display and ContainerGrid below.

    **2. Create onboarding page server** (`src/routes/kameras/onboarding/+page.server.ts`):
    Load the next available VMID for the wizard:
    ```typescript
    import type { PageServerLoad } from './$types';
    import { getNextVmid } from '$lib/server/services/onboarding';

    export const load: PageServerLoad = async () => {
      const nextVmid = await getNextVmid();
      return { nextVmid };
    };
    ```

    **3. Create onboarding page** (`src/routes/kameras/onboarding/+page.svelte`):
    ```svelte
    <script lang="ts">
      import OnboardingWizard from '$lib/components/onboarding/OnboardingWizard.svelte';
      let { data } = $props();
    </script>

    <h1 class="text-2xl font-bold text-text-primary mb-6">Kamera einrichten</h1>
    <OnboardingWizard nextVmid={data.nextVmid} />
    ```

    **4. Create StepIndicator** (`src/lib/components/onboarding/StepIndicator.svelte`):
    Props: `currentStep: number` (0-4), `steps: string[]` (5 labels).
    Default labels per D-02: `['Zugangsdaten', 'Verbindung testen', 'Container erstellen', 'go2rtc konfigurieren', 'Stream verifizieren']`.
    Render a horizontal bar with 5 circles connected by lines. Current step = accent color, completed = green, future = gray. Each circle has a number (1-5) and the label below.
    Use Tailwind: `flex items-center` for the bar. Circles: `w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold`. Lines: `flex-1 h-0.5`. Colors: completed `bg-green-500 text-white`, current `bg-accent text-white`, future `bg-surface-secondary text-text-secondary`.

    **5. Create OnboardingWizard** (`src/lib/components/onboarding/OnboardingWizard.svelte`):
    Props: `nextVmid: number`.
    State variables (Svelte 5 $state):
    - `currentStep = $state(0)` (0-indexed: 0=credentials, 1=test, 2=container, 3=configure, 4=verify)
    - `loading = $state(false)`
    - `error = $state<string | null>(null)`
    - `cameraId = $state<number | null>(null)`
    - Form data: `ip = $state('')`, `username = $state('admin')`, `password = $state('meinsm')`, `name = $state('')`
    - Transcode: `width = $state(1280)`, `height = $state(720)`, `fps = $state(20)`, `bitrate = $state(5000)`
    - Results: `connectionResult = $state<{resolution?: string, fps?: number, streamPath?: string} | null>(null)`
    - `containerIp = $state<string | null>(null)`
    - `rtspUrl = $state<string | null>(null)`
    - `streamInfo = $state<{active: boolean, codec: string|null, producers: number} | null>(null)`

    Layout: StepIndicator at top. Below it, conditionally render the current step component. Below the step, show InlineAlert if error is set.

    Step handler functions:
    - `handleCredentialsSubmit()`: Validate ip + username + password not empty. Call `POST /api/onboarding/test-connection` with {ip, username, password}. On success, store connectionResult, auto-fill width/height/fps from detected values if available (per D-12). Then create camera record: `POST` to a new save endpoint OR do it inline by calling saveCameraRecord via a new API route `/api/onboarding/save-camera`. Actually simpler: create a `/api/onboarding/save-camera` route that accepts all form data and returns `{ cameraId }`. Set `cameraId` state. Advance to step 1.

    Wait -- rethink the flow. The wizard should:
    1. Step 0 (Credentials): User fills form. On "Weiter" (next), save camera record to DB (status=pending) and immediately test connection. This combines D-11 (params in step 1) with D-02 step 2 (test).

    Actually per D-02, step 1 is credentials, step 2 is test. So:
    - Step 0: User fills form, clicks "Weiter". Save camera record to DB via fetch POST to `/api/onboarding/save-camera` with all form fields. Store returned cameraId. Advance to step 1.
    - Step 1: Auto-run test connection on mount. Show loading spinner. On success, update width/height/fps from detected values (if different from defaults). Show detected info. User clicks "Weiter" to advance.
    - Step 2: Auto-run create container on mount. Show loading spinner + status messages. On success, show containerIp. User clicks "Weiter".
    - Step 3: Auto-run configure go2rtc on mount. Show loading spinner + status messages. On success, show "go2rtc konfiguriert". User clicks "Weiter".
    - Step 4: Auto-run verify stream on mount. Show WebRTC iframe preview (per D-17). Show RTSP URL (per D-18). Show copy button. "Fertig" button returns to /kameras (per D-05).

    On error at any step (per D-04): Show InlineAlert with error message. Show "Erneut versuchen" (retry) button. User stays on current step.

    **Add one more API route** for saving the camera record: create `src/routes/api/onboarding/save-camera/+server.ts`:
    POST accepts `{ name, ip, username, password, width, height, fps, bitrate, vmid }`.
    Calls `saveCameraRecord(...)` from onboarding service. Returns `{ success: true, cameraId }`.

    **6. Create StepCredentials** (`src/lib/components/onboarding/StepCredentials.svelte`):
    Props (bindable): `ip`, `username`, `password`, `name`, `width`, `height`, `fps`, `bitrate`.
    Event prop: `onSubmit: () => void`.
    Form with labeled inputs per D-11:
    - Kamera-Name (text, required, placeholder "z.B. Einfahrt Mobotix")
    - Kamera-IP (text, required, placeholder "192.168.3.22")
    - Benutzername (text, required, default "admin")
    - Passwort (password, required, default "meinsm")
    - Separator/heading: "Transcode-Parameter (optional)" per D-11
    - Aufloesung Breite (number, default 1280)
    - Aufloesung Hoehe (number, default 720)
    - FPS (number, default 20)
    - Bitrate kbit/s (number, default 5000)
    Use Tailwind grid for layout. 2-column for IP+name, full width for credentials, 4-column for transcode params.
    "Weiter" button at bottom right, calls onSubmit.

    **7. Create StepTestConnection** (`src/lib/components/onboarding/StepTestConnection.svelte`):
    Props: `loading: boolean`, `error: string | null`, `connectionResult: object | null`, `onRetry: () => void`.
    Show loading spinner with "Verbindung wird getestet..." while loading.
    On success: show green checkmark + "Verbindung erfolgreich". Display detected resolution, fps if available. Show "Erkannte Parameter: {resolution} @ {fps}fps" text.
    On error: show InlineAlert with error + "Erneut versuchen" button calling onRetry.
    "Weiter" button (disabled while loading or on error).

    **8. Create StepCreateContainer** (`src/lib/components/onboarding/StepCreateContainer.svelte`):
    Props: `loading`, `error`, `vmid: number | null`, `containerIp: string | null`, `onRetry`.
    Show loading spinner with "Container wird erstellt..." while loading.
    On success: show "Container erstellt" with VMID and container IP.
    On error: InlineAlert + retry button.
    "Weiter" button.

    **9. Create StepConfigureGo2rtc** (`src/lib/components/onboarding/StepConfigureGo2rtc.svelte`):
    Props: `loading`, `error`, `onRetry`.
    Show loading spinner with "go2rtc wird konfiguriert..." then sub-steps:
    - "ffmpeg wird installiert..."
    - "go2rtc wird heruntergeladen..."
    - "Konfiguration wird bereitgestellt..."
    - "Service wird gestartet..."
    (These are cosmetic -- actual API call is one request. Use a simple progress text that changes on a timer.)
    On success: show "go2rtc erfolgreich konfiguriert".
    On error: InlineAlert + retry.
    "Weiter" button.

    **10. Create StepVerifyStream** (`src/lib/components/onboarding/StepVerifyStream.svelte`):
    Props: `loading`, `error`, `containerIp: string | null`, `streamName: string`, `rtspUrl: string | null`, `streamInfo: object | null`, `onRetry`, `onComplete`.
    Show loading spinner with "Stream wird verifiziert..." while loading.
    On success per D-17: Show iframe with `src="http://${containerIp}:1984/stream.html?src=${streamName}"`, width 640, height 360, in a rounded border container.
    Per D-18: Below iframe, show info panel with:
    - RTSP URL: `rtsp://<container-ip>:8554/<streamName>` with copy-to-clipboard button
    - Codec: from streamInfo
    - Status: "Aktiv" in green
    Per D-19: Note text "Diese URL kann in UniFi Protect als RTSP-Kamera hinzugefuegt werden."
    "Fertig" button calls onComplete which navigates to `/kameras` via `goto('/kameras')` (import from `$app/navigation`).
    On error: InlineAlert + retry.

    **All components use Tailwind dark theme classes** matching the existing app:
    - Background: `bg-surface-primary` or `bg-surface-secondary`
    - Text: `text-text-primary`, `text-text-secondary`
    - Accent: `bg-accent`
    - Inputs: `bg-surface-secondary border border-border rounded-lg px-3 py-2 text-text-primary`
    - Buttons: `bg-accent text-white rounded-lg px-4 py-2 hover:bg-accent/90`
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/ip-cam-master && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/kameras/+page.svelte contains `Kamera hinzuf`
    - src/routes/kameras/+page.svelte contains `href="/kameras/onboarding"`
    - src/routes/kameras/onboarding/+page.svelte contains `OnboardingWizard`
    - src/routes/kameras/onboarding/+page.server.ts contains `getNextVmid`
    - src/lib/components/onboarding/OnboardingWizard.svelte contains `currentStep`
    - src/lib/components/onboarding/OnboardingWizard.svelte contains `fetch('/api/onboarding/test-connection`
    - src/lib/components/onboarding/OnboardingWizard.svelte contains `fetch('/api/onboarding/create-container`
    - src/lib/components/onboarding/OnboardingWizard.svelte contains `fetch('/api/onboarding/configure-go2rtc`
    - src/lib/components/onboarding/OnboardingWizard.svelte contains `fetch('/api/onboarding/verify-stream`
    - src/lib/components/onboarding/StepIndicator.svelte contains `Zugangsdaten`
    - src/lib/components/onboarding/StepIndicator.svelte contains `Verbindung testen`
    - src/lib/components/onboarding/StepIndicator.svelte contains `Stream verifizieren`
    - src/lib/components/onboarding/StepCredentials.svelte contains `Kamera-IP`
    - src/lib/components/onboarding/StepCredentials.svelte contains `1280`
    - src/lib/components/onboarding/StepCredentials.svelte contains `5000`
    - src/lib/components/onboarding/StepTestConnection.svelte contains `Verbindung wird getestet`
    - src/lib/components/onboarding/StepTestConnection.svelte contains `Erneut versuchen`
    - src/lib/components/onboarding/StepCreateContainer.svelte contains `Container wird erstellt`
    - src/lib/components/onboarding/StepConfigureGo2rtc.svelte contains `go2rtc wird konfiguriert`
    - src/lib/components/onboarding/StepVerifyStream.svelte contains `stream.html?src=`
    - src/lib/components/onboarding/StepVerifyStream.svelte contains `iframe`
    - src/lib/components/onboarding/StepVerifyStream.svelte contains `rtsp://`
    - src/lib/components/onboarding/StepVerifyStream.svelte contains `Fertig`
    - src/routes/api/onboarding/save-camera/+server.ts contains `export const POST`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    Complete 5-step onboarding wizard accessible from Kameras page via "+Kamera hinzufuegen" button. Wizard has progress indicator showing all 5 steps (per D-03). Each step auto-executes its API call on entry, shows loading state, and provides retry on error (per D-04). Step 1 collects credentials + transcode params with smart defaults (1280x720, 20fps, 5000kbit/s per D-13). Step 5 shows WebRTC iframe preview (per D-17) and displays RTSP URL with copy button (per D-18). "Fertig" button returns to Kameras page (per D-05). Project builds cleanly.
  </done>
</task>

</tasks>

<verification>
Build succeeds:
```bash
npm run build
```
Full test suite still passes:
```bash
npx vitest run
```
</verification>

<success_criteria>
- "+Kamera hinzufuegen" button visible on Kameras page linking to /kameras/onboarding
- 5-step wizard renders with progress indicator
- Each step component exists with correct content
- WebRTC iframe in step 5 uses go2rtc stream.html
- RTSP URL displayed in step 5
- All existing tests still pass
- Project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-mobotix-camera-pipeline/02-02-SUMMARY.md`
</output>

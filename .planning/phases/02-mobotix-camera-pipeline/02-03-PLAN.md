---
phase: 02-mobotix-camera-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false
requirements:
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-06

must_haves:
  truths:
    - "User can navigate the complete onboarding wizard from Kameras page through all 5 steps"
    - "Wizard UI matches the locked decisions: progress indicator, retry on error, return to Kameras"
    - "All components render correctly in the dark theme"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete Mobotix onboarding wizard UI.

Purpose: Confirm the wizard flow works visually and interactively before marking Phase 2 as complete. This is the human checkpoint to verify the UI matches the locked decisions (D-01 through D-19).

Output: User approval of the wizard UI and flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mobotix-camera-pipeline/02-CONTEXT.md
@.planning/phases/02-mobotix-camera-pipeline/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of onboarding wizard</name>
  <files></files>
  <action>
    Human visually verifies the complete 5-step Mobotix camera onboarding wizard built in Plans 01 and 02.

    What was built:
    - "+Kamera hinzufuegen" button on Kameras page (D-01)
    - 5-step progress indicator (D-03)
    - Step 1: Camera IP, credentials, transcode parameters with defaults (D-11, D-13)
    - Step 2: Connection test with detected resolution display (D-12)
    - Step 3: Container creation with VMID and IP display
    - Step 4: go2rtc configuration with progress messages (D-07, D-08, D-09, D-10)
    - Step 5: WebRTC live preview iframe + RTSP URL with copy button (D-17, D-18, D-19)
    - Error handling with retry at each step (D-04)
    - Return to Kameras page on completion (D-05)

    How to verify:
    1. Start the dev server: `npm run dev`
    2. Open http://localhost:5173/kameras
    3. Verify "+Kamera hinzufuegen" button is visible in the top-right area
    4. Click the button -- should navigate to /kameras/onboarding
    5. Verify the 5-step progress indicator is displayed at the top with step labels in German
    6. Verify Step 1 form shows: Kamera-Name, Kamera-IP, Benutzername (default "admin"), Passwort (default "meinsm"), and transcode parameters section with defaults (1280, 720, 20, 5000)
    7. Verify the UI uses dark theme consistently (no white backgrounds or broken styling)
    8. (Optional) If a Proxmox host is available: fill in real camera details and walk through all 5 steps to verify the complete flow
    9. Verify step transitions, loading states, and error/retry behavior look correct

    NOTE: Full end-to-end testing requires a real Proxmox host with a Mobotix camera. The visual checkpoint focuses on UI rendering and flow logic.
  </action>
  <verify>Human confirms wizard UI renders correctly by typing "approved"</verify>
  <done>User approves the visual appearance and flow of the onboarding wizard. All 5 steps render with correct German labels in dark theme.</done>
</task>

</tasks>

<verification>
User confirms the wizard UI renders correctly and matches the locked decisions.
</verification>

<success_criteria>
- User approves the visual appearance and flow of the onboarding wizard
- All 5 steps render with correct German labels
- Dark theme is consistent
- Progress indicator shows step progression
- Error/retry UI pattern is visible
</success_criteria>

<output>
After completion, create `.planning/phases/02-mobotix-camera-pipeline/02-03-SUMMARY.md`
</output>

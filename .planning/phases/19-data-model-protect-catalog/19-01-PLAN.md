---
phase: 19-data-model-protect-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v1.3/spikes/p19-tls-rtspx.md
autonomous: false
requirements:
  - HUB-CAT-02
user_setup:
  - service: unifi-protect
    why: "TLS spike requires one Protect cam to have its 'Share Livestream → Enable Secure RTSPS Output' toggle ON before the throwaway LXC can run ffprobe against a real alias. Without this, the spike has no real share URL to probe and the whole P19 plan-set blocks."
    dashboard_config:
      - task: "Open UniFi Protect web UI at https://192.168.3.1, pick any one camera, open its 'Streams' / 'RTSP' settings tab, and turn ON the 'Share Livestream' toggle (also called 'Enable Secure RTSPS Output' in some firmwares). Copy the resulting `rtspx://192.168.3.1:7441/<rtspAlias>?enableSrtp` URL — Plan 01 Task 02 needs the `<rtspAlias>` token."
        location: "UniFi Protect web UI → camera detail → Streams tab"

must_haves:
  truths:
    - "A real ffprobe run was performed against rtspx://192.168.3.1:7441/<alias>?enableSrtp from a throwaway LXC against the user's actual UDM"
    - "A second ffprobe run was performed against rtsps://192.168.3.1:7441/<alias>?enableSrtp with -tls_verify 0 from the same LXC"
    - "The findings document records both raw outputs verbatim, picks one scheme, and is committed to git"
    - "The spike LXC was destroyed after the run — leaves no residue on Proxmox"
    - "P21 / P23 implementers can read the findings file and know exactly which TLS_SCHEME to import without re-running probes"
  artifacts:
    - path: ".planning/research/v1.3/spikes/p19-tls-rtspx.md"
      provides: "TLS scheme audit trail (rtspx:// vs rtsps:// + tls_verify=0). Contains a top-level `Result: <CHOSEN SCHEME>` line with value either `rtspx` or `rtsps-tls-verify-0`."
      contains: "Result: rtspx | Result: rtsps-tls-verify-0"
  key_links:
    - from: ".planning/research/v1.3/spikes/p19-tls-rtspx.md"
      to: "src/lib/server/services/protect-bridge.ts (Plan 03)"
      via: "Plan 03 Task 02 reads the `Result:` line and hard-codes `TLS_SCHEME` const accordingly"
      pattern: "Result: (rtspx|rtsps-tls-verify-0)"
---

<objective>
Run the P19 TLS spike against the user's actual UDM at 192.168.3.1 from a throwaway Proxmox LXC, capture both ffprobe variants verbatim, choose the working scheme, and commit the findings to `.planning/research/v1.3/spikes/p19-tls-rtspx.md`. This file is the single source of truth that Plan 03 reads to lock the `TLS_SCHEME` const in `protect-bridge.ts`.

Purpose: The locked decision D-TLS-01 / D-TLS-02 mandates this spike must happen FIRST and its result must be a const, not a runtime probe. Every later phase (P21 yaml-builder, P23 share-toggle) imports the const. Without this empirical evidence, the rest of v1.3 is built on guesswork about whether `rtspx://` works against UDM 192.168.3.1.

Output: A committed markdown findings file. No source code is touched in this plan. The const lock happens in Plan 03 once Plan 03 reads this file.

Scope boundary: This plan does NOT touch `package.json`, `protect-bridge.ts`, or any source files. It does not depend on Plan 02. It can run in Wave 1 in parallel with Plan 02 because the only file it modifies is the new spike-findings markdown file (zero overlap with Plan 02's `files_modified`).
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-data-model-protect-catalog/19-CONTEXT.md
@.planning/phases/19-data-model-protect-catalog/19-RESEARCH.md
@.planning/phases/19-data-model-protect-catalog/19-PATTERNS.md

<interfaces>
<!-- Existing primitives this plan reuses for LXC provisioning + SSH execution. The executor uses these directly — no codebase exploration needed. -->

From src/lib/server/services/proxmox.ts (verified lines 83–149):
```typescript
export async function getProxmoxClient(): Promise<ReturnType<typeof proxmoxApi>>;
export async function getNodeName(proxmox?: ReturnType<typeof proxmoxApi>): Promise<string>;
export async function createContainer(params: {
  vmid: number;
  hostname: string;
  ostemplate: string;
  memory?: number;
  cores?: number;
  cameraName?: string;
  cameraIp?: string;
  cameraType?: string;
}): Promise<{ status: 'created' | 'updated'; vmid: number }>;
```

From src/lib/server/services/ssh.ts (existing primitive used by Phase 5 + 18 for in-LXC commands):
```typescript
// node-ssh wrapper — execute commands inside an LXC via `pct exec` or via direct ssh
// (Phase 18 used `pct exec` for Bambu A1; we use the same pattern here.)
```

Spike-findings file template (verbatim from RESEARCH.md §"Findings file structure"):
```markdown
# P19 Spike — TLS Scheme for Protect RTSPS Streams

**Date:** 2026-04-30
**Run by:** P19 Plan 01 Task 02 (automated)
**Target UDM:** 192.168.3.1 (firmware version: <captured from Protect bootstrap>)
**Test alias:** <rtspAlias of one shared cam>

## Result: <CHOSEN SCHEME>

`TLS_SCHEME = '<rtspx | rtsps-tls-verify-0>'`

## Variant A — `rtspx://`
Command: `<exact command>`
Result: <SUCCESS | FAILURE>
Output: ```<full ffprobe stderr>```

## Variant B — `rtsps://` + `-tls_verify 0`
Command: `<exact command>`
Result: <SUCCESS | FAILURE>
Output: ```<full ffprobe stderr>```

## Decision Rationale
<2-3 sentences. If both succeeded, prefer `rtspx://`. If only B succeeded, use B + `-tls_verify 0`.>

## Implications for Later Phases
- P21 yaml-builder imports `TLS_SCHEME` from `protect-bridge.ts`.
- P23 share-toggle uses the same scheme when probing channel availability post-toggle.

## Reproducibility
- Spike LXC was destroyed after run; vmid recorded above for audit.
- Re-running this spike requires a Protect cam with active "Enable Secure RTSPS Output" toggle.
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 01: Confirm one Protect cam has 'Share Livestream / Enable Secure RTSPS Output' toggled ON</name>
  <read_first>
    - .planning/phases/19-data-model-protect-catalog/19-RESEARCH.md (§"TLS Spike Plan-Task" → step 1 "Pre-step (no spike code yet)")
    - .planning/phases/19-data-model-protect-catalog/19-CONTEXT.md (§"TLS Spike (D-TLS)")
  </read_first>
  <what>
    The throwaway LXC needs a real `rtspAlias` to probe. The spike cannot fabricate one — Protect generates the alias only when a camera owner flips the share toggle.
  </what>
  <how-to-verify>
    1. Open https://192.168.3.1 in a browser, log into UniFi Protect.
    2. Pick any one adopted camera (any model — UVC or third-party both work for the spike).
    3. Open the camera's "Streams" / "RTSP" settings tab.
    4. Toggle ON "Share Livestream" (also labeled "Enable Secure RTSPS Output" in firmware ≥3.0).
    5. Copy the resulting `rtspx://192.168.3.1:7441/<TOKEN>?enableSrtp` URL — capture the `<TOKEN>` portion only.
    6. Paste the token in your reply along with the camera name.
  </how-to-verify>
  <resume-signal>Reply with `rtspAlias=<TOKEN> camName=<NAME>` so subsequent automated tasks can substitute the real alias into ffprobe commands. If you cannot toggle the share for any reason (e.g. UDM unreachable), reply `BLOCKED: <reason>` so the planner can offer alternatives.</resume-signal>
  <acceptance_criteria>
    - User has replied with `rtspAlias=<token>` (alphanumeric, ~32 chars)
    - User confirms the share toggle is ON in the Protect UI
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 02: Provision throwaway LXC, run both ffprobe variants, write findings file, destroy LXC, commit</name>
  <files>
    .planning/research/v1.3/spikes/p19-tls-rtspx.md
  </files>
  <read_first>
    - src/lib/server/services/proxmox.ts (lines 83–149 — `createContainer()`, `getNodeName()`, `getProxmoxClient()`)
    - src/lib/server/services/ssh.ts (existing helper — exact API surface for `pct exec`-style command execution; mirror Phase 18's Bambu LXC test pattern)
    - src/lib/server/services/go2rtc.ts → search for `getInstallCommands` (the existing helper that prepares `apt-get install ffmpeg` for an LXC; reuse for the spike LXC's ffmpeg install)
    - .planning/phases/19-data-model-protect-catalog/19-RESEARCH.md (§"TLS Spike Plan-Task" steps 2–9)
  </read_first>
  <action>
    Build a one-shot Node.js script (executed via `tsx scripts/spike/p19-tls-rtspx.ts`) that performs the spike end-to-end. The script must:

    1. **Provision a throwaway LXC** by calling `createContainer({ vmid: 9919, hostname: 'p19-tls-spike', ostemplate: <existing Debian-13 template path from settings.proxmox_ostemplate>, memory: 512, cores: 1, cameraName: 'p19-spike', cameraType: 'spike' })`. Wait for `start()` to complete and the container to acquire a DHCP IP (poll `pct config 9919` for `net0` until `ip=...` resolves; max 60 s).

    2. **Install ffmpeg in the LXC** via `pct exec 9919 -- bash -c 'apt-get update -qq && apt-get install -y -qq ffmpeg'`. Capture and store the ffmpeg version banner: `pct exec 9919 -- ffmpeg -version | head -1`.

    3. **Substitute the user-supplied alias** from Task 01's `resume-signal`. The variable `RTSP_ALIAS` is the `<TOKEN>` value the user provided.

    4. **Run Variant A — rtspx://** (verbatim command):
       ```bash
       pct exec 9919 -- bash -c 'ffprobe -v info -show_format -show_streams -i "rtspx://192.168.3.1:7441/'$RTSP_ALIAS'?enableSrtp" -timeout 5000000 2>&1 | tee /tmp/rtspx-probe.txt'
       ```
       Capture the full stderr+stdout into a string variable `variantA_output`. Mark `variantA_result = 'SUCCESS'` if a `Stream #0:0: Video: h264` (or `h265`) line appears in the output AND there is no `401 Unauthorized` / `Certificate verify failed` / `Connection refused` error. Otherwise `variantA_result = 'FAILURE'`.

    5. **Run Variant B — rtsps:// + -tls_verify 0** (verbatim command):
       ```bash
       pct exec 9919 -- bash -c 'ffprobe -v info -show_format -show_streams -tls_verify 0 -i "rtsps://192.168.3.1:7441/'$RTSP_ALIAS'?enableSrtp" -timeout 5000000 2>&1 | tee /tmp/rtsps-probe.txt'
       ```
       Same success/failure detection logic. Store as `variantB_output` / `variantB_result`.

    6. **Choose the scheme**:
       - If `variantA_result === 'SUCCESS'` → `chosen_scheme = 'rtspx'`.
       - Else if `variantB_result === 'SUCCESS'` → `chosen_scheme = 'rtsps-tls-verify-0'`.
       - Else → write the findings file with `Result: BOTH FAILED` + both raw outputs and exit with code 1 (this BLOCKS Plan 03, by design — the user must investigate).

    7. **Write findings file** at `.planning/research/v1.3/spikes/p19-tls-rtspx.md` using the exact template from RESEARCH.md (reproduced in `<interfaces>` above). Substitute:
       - `<CHOSEN SCHEME>` → value of `chosen_scheme` (`rtspx` or `rtsps-tls-verify-0`)
       - `<rtspAlias of one shared cam>` → the alias the user provided (NOT the full URL; just the token)
       - `<exact command>` → the ffprobe commands above (with `$RTSP_ALIAS` already substituted)
       - `<full ffprobe stderr>` → full output from each probe (do NOT truncate; this is the audit trail)
       - `<firmware version>` → leave as `(not captured — pre-bootstrap)` since we don't have a working Protect client yet (Plan 03 will populate it on the next bootstrap call). Acceptable.
       - **Top-level `Result:` line MUST be exactly:** `Result: rtspx` OR `Result: rtsps-tls-verify-0` — Plan 03 Task 02 grep-extracts this exact line.

    8. **Destroy the spike LXC**: `pct stop 9919 && pct destroy 9919 --purge`. Verify destruction with `pct list` returning no row for vmid 9919.

    9. **Commit** the findings file: `git add .planning/research/v1.3/spikes/p19-tls-rtspx.md && git commit -m "docs(19-01): P19 TLS spike findings — chosen scheme: <chosen_scheme>"` (message body includes the chosen scheme so it's grep-able from `git log`).

    **Important boundaries:**
    - Do NOT touch `src/lib/server/services/protect-bridge.ts` — that file does not exist yet; Plan 03 creates it.
    - Do NOT modify `package.json` — Plan 02 owns dep installs.
    - Do NOT leave the LXC running. The spike is one-shot; cleanup is mandatory.
    - The script lives at `scripts/spike/p19-tls-rtspx.ts` (or wherever feels right under `scripts/`); it does not need to be unit-tested — it's a one-time investigation tool. Delete the script after the spike if you prefer, but committing it is fine for reproducibility.
  </action>
  <verify>
    <automated>test -f .planning/research/v1.3/spikes/p19-tls-rtspx.md &amp;&amp; grep -E '^Result: (rtspx|rtsps-tls-verify-0|BOTH FAILED)$' .planning/research/v1.3/spikes/p19-tls-rtspx.md &amp;&amp; (pct list 2>/dev/null | grep -v 9919 || true) &amp;&amp; git log -1 --pretty=format:'%s' | grep -F '19-01' | grep -F 'TLS spike findings'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/v1.3/spikes/p19-tls-rtspx.md` exists
    - First H2 section header is `## Result: <value>` where value is one of `rtspx`, `rtsps-tls-verify-0`, or `BOTH FAILED`
    - File contains both Variant A and Variant B raw ffprobe outputs (verbatim — at least one Stream/Error line in each)
    - File contains `## Decision Rationale` section with 2-3 sentences
    - LXC vmid 9919 no longer appears in `pct list` output (destruction confirmed)
    - The findings file is committed to git on the current branch (verify via `git log -1 --pretty=format:'%H %s'` showing the docs commit)
    - If `Result: BOTH FAILED`, the executor's COMPLETE marker explicitly says "BLOCKED — both variants failed; user must investigate UDM port 7441 / firewall / share-toggle state before P19 can proceed"
  </acceptance_criteria>
  <done>
    Spike findings committed; chosen TLS scheme is locked in the markdown file. Plan 03 Task 02 will grep this file's `Result:` line to set the `TLS_SCHEME` const value in `protect-bridge.ts`. No source code changed in this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App process → Proxmox API | App holds Proxmox API token; LXC create/destroy is privileged |
| Spike LXC → UDM (192.168.3.1) | UDM exposes :7441 RTSPS; spike LXC is on the same trusted LAN segment |
| App process → spike LXC | `pct exec` runs as root inside the LXC; only the app process / operator should invoke it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Information Disclosure | Spike LXC ffprobe output captured to git | accept | The `<rtspAlias>` token in the URL is a Protect share secret. Mitigation: the findings file is in a private repo (`.planning/` is not published). If the repo is ever made public, redact the alias. Documented in the findings file's "Reproducibility" section (sensitive token included for audit). |
| T-19-01-02 | Denial of Service | Throwaway LXC consuming Proxmox storage | mitigate | Cleanup is mandatory (Task 02 step 8); `pct list` verification is in `<acceptance_criteria>`. |
| T-19-01-03 | Tampering | Manipulated findings file (claiming `rtspx` works when it does not) | accept | The plan-checker can rerun the same probe against the same alias for verification. Spike output is verbatim, not interpreted. Low-value attack target (single-user homelab tool). |
| T-19-01-04 | Elevation of Privilege | Spike LXC has no `/dev/dri` passthrough → no transcode capability → cannot exfiltrate frame data even if compromised | mitigate | Sized 512 MB / 1 core / no GPU; ffprobe-only scope. |
</threat_model>

<verification>
- `.planning/research/v1.3/spikes/p19-tls-rtspx.md` exists with valid `Result:` header
- LXC vmid 9919 destroyed
- Findings committed to git
- Plan 03 can read the `Result:` line via `grep -E '^Result:' .planning/research/v1.3/spikes/p19-tls-rtspx.md | awk '{print $2}'`
</verification>

<success_criteria>
- TLS spike findings file committed at `.planning/research/v1.3/spikes/p19-tls-rtspx.md`
- File's `Result:` line is one of: `rtspx`, `rtsps-tls-verify-0`, or (blocking) `BOTH FAILED`
- Throwaway LXC destroyed
- Satisfies ROADMAP §Phase 19 Success Criterion #6 (spike artifact committed)
</success_criteria>

<output>
After completion, create `.planning/phases/19-data-model-protect-catalog/19-01-SUMMARY.md` documenting:
- Chosen TLS scheme
- Probe duration
- Any anomalies (firewall surprises, firmware quirks, alias-token format)
- Whether the spike LXC's vmid (9919) is now safe to reuse for future spikes
</output>
